package net.chrigel.clustercode.constraint.impl;

public class ConstraintConfiguration {

    /**
     * Defines the key for the file size constraint (min size)
     */
    public static final String CONSTRAINT_FILE_MIN_SIZE_KEY = "CONSTRAINT_FILE_MIN_SIZE";
    /**
     * Defines the key for the file size constraint (max size)
     */
    public static final String CONSTRAINT_FILE_MAX_SIZE_KEY = "CONSTRAINT_FILE_MAX_SIZE";

    /**
     * Defines the key for the time constraint (begin time)
     */
    public static final String CONSTRAINT_TIME_BEGIN_KEY = "CONSTRAINT_TIME_BEGIN";

    /**
     * Defines the key for the time constraint (stop time)
     */
    public static final String CONSTRAINT_TIME_STOP_KEY = "CONSTRAINT_TIME_STOP";

}
