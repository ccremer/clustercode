version '1.0.0'

dependencies {
    compile project(":clustercode.api.domain")
    compile project(":clustercode.impl.util")
    compile project(":clustercode.api.config")
    compile "${dep_owner}"
    compile "${dep_inject}"
}
